1.Install json-server with the command "npm install json-server -g" 
2.Run the json-server with "json-server db.json --watch -p 3001" 

[NOTE]!For solving the error use:
    "Set-ExecutionPolicy -ExecutionPolicy Bypass -Scope CurrentUser"
